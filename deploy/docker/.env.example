# 城市时域生活圈分析系统 - Docker 部署密钥配置模板
# 复制为 .env (与本文件同目录), docker compose 会自动读取, 不会被提交进 git。
# 两个 key 均可选: 留空时对应功能自动降级 (底图回退 OSM / 搜索地址不可用), 核心分析不受影响。

# 高德开放平台 "Web 服务" 类型 key (后端地理编码/逆地理编码用)
# 建议在控制台为其设置 IP 白名单 = 服务器公网 IP
AMAP_KEY=你的高德Web服务key

# 天地图 "浏览器端" key (前端底图用), 留空则回退 OSM 底图
# 建议在控制台为其设置域名/referer 白名单 = 你的站点域名
VITE_TIANDITU_KEY=你的天地图浏览器key

# npm 镜像源 (国内服务器构建镜像时 npm install 更快的源)
# NPM_REGISTRY=https://registry.npmmirror.com

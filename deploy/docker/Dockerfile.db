# 数据库镜像: PostgreSQL 13 + PostGIS 3 + pgRouting 4
FROM postgis/postgis:13-3.3
RUN apt-get update \
    && apt-get install -y --no-install-recommends postgresql-13-pgrouting \
    && rm -rf /var/lib/apt/lists/*

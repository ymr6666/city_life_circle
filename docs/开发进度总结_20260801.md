# 城市时域生活圈分析系统 — 开发进度总结

> 生成日期：2026-08-01
> 说明：本文记录**截至本次会话结束**的开发完成度、数据现状与未完成事项。
> 与 `docs/项目总结.md`（总体设计）配合阅读；本文侧重"当前进度快照"。

---

## 一、本次会话完成的工作

### 1.1 路网数据增强：桥隧四档分类 + 模式可达列 ✅

**背景问题**：原 `hefei_roads` 只有 osmnx 默认保留的少量 tag，`bridge=yes` 无法区分"可走的过河桥"与"不能上的立交高架"；行人/骑行/驾车共用一套散落的过滤 SQL，且不一致。

**已完成动作**：

1. **增量抓取缺失 OSM tag**（`scripts/utils/enrich_roads_tags.py`）
   - 通过 Overpass 按 way id 分批拉取：`layer / foot / bicycle / motor_vehicle / vehicle / access / sidewalk / cycleway / surface / smoothness / lit / segregated / oneway:bicycle / incline`
   - 缓存：`cache/roads_tags.json`（21,412 条 way，约 85% 覆盖；其余为已删除的旧 way，走回退分类）
   - 支持 osmnx 合并后的 `[a,b,c]` 多 way osmid 聚合（access 类取最严格、layer 取最大、bridge/tunnel 取任一 yes）

2. **hefei_roads 新增列**：
   - 原始 tag 列：`layer, foot, bicycle, motor_vehicle, vehicle, access, sidewalk, cycleway, surface, smoothness, lit, segregated, oneway_bicycle, incline`
   - 派生列：`bridge_class`（四档物理结构）、`walk_ok / cycle_ok / drive_ok`（布尔可达列）

3. **四档桥隧分类规则**（`bridge_class`）：
   | 档位 | 判定 | walk | cycle | drive |
   |------|------|------|-------|-------|
   | `elevated` 立交高架 | bridge + layer>0（或 primary/secondary 无 layer 回退） | ✗ | ✗ | ✓ |
   | `tunnel` 车行隧道 | tunnel=yes + 车行等级 | ✗ | ✗ | ✓ |
   | `footbridge` 步行天桥 | bridge + footway/pedestrian | ✓ | ✗ | ✗ |
   | `bridge` 普通过河桥 | bridge + 低等级道路 | ✓ | ✓ | ✓ |
   | `none` | 非桥隧 | — | — | — |

4. **分类实测统计**（总边 104,130，cost>0）：
   ```
   walk  98,055 (94%)    cycle 97,506 (94%)    drive 88,489 (85%)
   bridge_class: none 100,148 | elevated 3,066 | tunnel 517 | bridge 237 | footbridge 162
   ```
   已抽样验证：motorway/trunk 禁行、普通住宅路全可走、天桥 walk可走 cycle禁行、高架禁行、车行隧道禁行、步行隧道可走等，无误伤。

### 1.2 引擎重构 + 统一多模式架构 ✅

**覆盖全部使用方式**（本次按需求设计并实现）：
```
单独道路:  walk | cycle | drive
道路+公交: walk+metro | walk+bus | walk+metro+bus   (metro/bus 为别名)
```

- **`backend/engine/walk_layer.py`**：删除散落的 `WALK_HIGHWAY_FILTER`，改为按 `hefei_roads.{ok_column}` 过滤；抽出 `ok_column / directed / poi_mode` 类属性，便于复用。
- **`backend/engine/cycle_layer.py`**：骑行层 `CycleLayer`（15 km/h、`cycle_ok`、`directed=True` 尊重单行道、排除 steps 与步行天桥）。`_edge_sql` 改用 `self.ok_column`。
- **`backend/engine/drive_layer.py`（新增）**：驾车层 `DriveLayer`（30 km/h、`drive_ok`、单行道、`poi_mode='drive'`）。立交/隧道不排除（机动车需上桥），"只能从匝道上下"由拓扑保证。
- **`backend/engine/transit_layer.py`（新增）**：**通用耦合引擎 `TransitLayer`**——统一图（道路边 + 地铁/公交边 + 换乘边），成本统一为分钟，单次 Dijkstra。
  - 节点偏移：路网 1~42785 / 地铁站 100000+ / 公交站 200000+stop_no
  - 公交边：`hefei_bus_line_stops` 相邻站连线，时间=直线距离/20km/h（`BUS_SPEED_KMH`）
  - **数据就绪检查**：bus 相关表缺失时抛 `ValueError` 明确提示（先 `--load` 再 `snap_bus_stops`）
  - 方向：walk 接驳 `directed=false`；cycle/drive 接驳 `directed=true` 尊重单行道
- **`backend/engine/metro_layer.py`**：重构为 `TransitLayer` 子类（road=walk, transit=('metro',)），输出字段向后兼容。
- **`backend/engine/bus_layer.py`（新增）**：`BusLayer`（walk+bus）、`WalkMetroBusLayer`（walk+metro+bus）。
- **`backend/engine/factory.py`（新增）**：`build_layer(mode)` 解析复合模式（`walk+metro+bus`、别名 `metro`/`bus`、非法输入报错）。
- **`backend/routes/isochrone.py`**：API 支持上述全部 mode；非法→400，公交数据未就绪→503。
- **`backend/test_walk.py`**：用 factory 构建层，通用输出（含公交站列表）。

**实测（市中心 31.861, 117.285）**：
| 模式 | 预算 | 可达路网节点 | 地铁/公交 | 可达 POI |
|------|------|-------------|----------|---------|
| walk 15min | 1,250m | 322 | — | 116 |
| cycle 15min | 3,750m | 1,516 | — | 362 |
| drive 15min | 7,500m | 5,551 | — | 2,165 |
| metro 30min | — | 8,704 | 126 站 | 2,530 |
| walk+metro 30min | — | 8,704 | 126 站 | 2,530 |
| bus / walk+bus / walk+metro+bus | — | — | — | 503（数据未就绪，待抓取） |

### 1.3 公交爬虫重写（适配真实配额） ✅

**真实约束**（个人认证实测）：公交接口 **日配额 100 次、限速 1 req/s**；合肥 4,000+ 站点、300+ 线路。

**`scripts/crawlers/crawl_bus.py` 重写要点**：
| 项 | 说明 |
|----|------|
| 限速 | `SLEEP = 1.1`（1 req/s 留余量） |
| 日配额 | `DAILY_QUOTA = 100`，默认 `run_daily()` 在预算内自动分配 Phase2/Phase3 |
| 配额保护 | `infocode 10003/10004` → 抛 `QuotaExhausted` 立即停止，**不烧配额重试**，先保存 checkpoint |
| 断点续跑 | 只有 `status=1` 成功才标记完成，失败项次日自动重查 |
| 饱和早停 | 连续 30 站无新线路 → 停止 Phase2（避免把 1,068 站全查完） |
| 坐标 | 落库前 GCJ-02 → WGS84（高德数据偏移合肥约 570m，路网为 WGS84） |

**原高德配额方案（已弃用）**：高德公交接口日配额仅 100 次、限速 1 req/s，逐线 `linename&extensions=all` 需 ~900 次 ≈ 9 天，不现实。

**改用免费方案（✅ 已完成）**：
| 数据 | 来源 | 状态 |
|------|------|------|
| 站点坐标（5,530，WGS84） | 官方接口 `61.133.142.137`（`download_hfbus_official.py`） | ✅ |
| 线路清单（898 去重） | 官方接口（同上下载） | ✅ |
| **有序站点序列（去/回程）** | 8684 爬虫（`crawl_8684.py`，276 线路命中、227 条含站序） | ✅ |
| 站名→官方坐标匹配 | `load_8684_bus.py`（去括号精确 + difflib 模糊，**匹配率 94.3%**） | ✅ |
| 公交站→路网吸附 | `snap_bus_stops.py`（4,932/5,530 站 = 89%） | ✅ |

**落库结果**：`hefei_bus_stops` 5,530 | `hefei_bus_lines` 227 | `hefei_bus_line_stops` 8,489（覆盖 225 线路，含 direction 与 route_pos_m）。

**实测（市中心 31.861, 117.285）**：
| 模式 | 路网节点 | 地铁站 | 公交站 | POI |
|------|---------|--------|--------|-----|
| walk+bus 30min | 10,434 | — | 2,217 | 2,934 |
| walk+metro+bus 30min | 15,477 | 128 | 2,961 | 3,682 |

**`crawl_bus.py`（高德版）已废弃**，保留作参考；`bus_amap_probe.json` 记录了高德数据质量评估（lineid 可给站序，但配额受限）。

---

## 二、未完成 / 待办事项

### 2.1 公交数据（✅ 已完成）
免费方案已打通全部 6 种使用方式（walk/cycle/drive/walk+metro/walk+bus/walk+metro+bus）。后续可增强：
- 8684 未命中的县域/通达线路（422 条，多在分析 bbox 外），如需可再补
- 公交等车时间建模（当前 0 等待，直接上车）

### 2.2 项目总体规划中的未开发模块（参照 `docs/项目总结.md`）
| 优先级 | 模块 | 状态 |
|--------|------|------|
| ⭐⭐⭐ | POI-路网挂接 `poi_road_nodes` | ✅ 已有（walk/drive 两种模式；骑行复用 walk） |
| ⭐⭐⭐ | `/api/isochrone` | ✅ walk/cycle/drive/metro/walk+metro/bus 全部模式就绪（bus 待数据） |
| ⭐⭐⭐ | `/api/geocode` 地理编码 | ❌ |
| ⭐⭐⭐ | `/api/poi-stat`（多边形内分类统计） | ❌ |
| ⭐⭐⭐ | `/api/score` 评分系统 | ❌ |
| ⭐⭐ | 反算 `/api/reverse-isochrone`（设施→覆盖范围，多源 Dijkstra） | ❌ 已定方案 |
| ⭐⭐ | 前端 Vue 3 + 地图展示 | ❌（frontend/ 为空） |
| ⭐ | 文档/PPT/演示 | 部分 |

### 2.3 后续增强（已讨论，未实施）
- **公交**：等待时间建模（固定等车时间即可，避免引入时变图）；OSM 公交覆盖差，数据源以高德为准
- **路网**：`access=private` 目前从步行/骑行中排除（小区内部路），如需"从家出发"可再评估
- **骑行**：可用已抓取的 `oneway_bicycle` 字段做"单行道允许非机动车逆行"的细化
- **限号/时段限制**：留 cost 修正接口，后续补
- **建筑轮廓**：入库后 simplify + 分级渲染，用于前端展示与面积统计（与 POI 为模糊匹配，不做精确路径对接）

---

## 三、当前数据现状

数据库：`city_life_circle`（PostgreSQL + PostGIS + pgRouting）

| 表 | 内容 | 备注 |
|----|------|------|
| `hefei_roads` | 104,130 路段 | 含 14 个新 tag 列 + bridge_class + walk/cycle/drive_ok |
| `hefei_roads_vertices_pgr` | 42,785 顶点 | 拓扑已建 |
| `hefei_poi` | 6,567 POI | 12 分类 |
| `hefei_metro_stations` | 169 站 | |
| `hefei_metro_edges` | 183 轨道边 | |
| `poi_road_nodes` | POI↔路网节点挂接 | mode=walk/drive |
| `metro_station_road_nodes` | 地铁站↔路网节点 | |
| `hefei_bus_stops` | 5,530 公交站 | 官方 WGS84 + `stop_no` 节点号 |
| `hefei_bus_lines` | 227 公交线路 | 8684 站序线路 + 官方元数据 |
| `hefei_bus_line_stops` | 8,489 条站序 | 含 `direction`(up/down) + `route_pos_m` |
| `bus_stop_road_nodes` | 14,013 吸附记录 | 覆盖 4,932 站（89%） |

坐标体系：**路网/地铁/公交 = WGS84**；**高德 POI = GCJ-02**（合肥偏移 ~570m），POI 现按原样入库（挂接容错处理）。

缓存文件（`cache/`）：
- `roads_tags.json`（OSM way tag 缓存，21,412 条）
- `bus_stops_official.json`（官方 5,530 站）
- `bus_lines_official.json`（官方 17,637 线路记录）
- `bus_8684_enum.json` / `bus_8684_lines.json`（8684 线路 hash + 详情）
- `bus_amap_probe.json`（高德质量评估，备查）

---

## 四、常用命令速查

```bash
# 后端启动
cd backend && python app.py

# 可达性测试（支持 walk/cycle/drive/metro/bus/walk+metro/walk+metro+bus）
cd backend && python test_walk.py 31.861 117.285 15 --mode walk
cd backend && python test_walk.py 31.861 117.285 15 --mode cycle
cd backend && python test_walk.py 31.861 117.285 15 --mode drive
cd backend && python test_walk.py 31.861 117.285 30 --mode walk+metro
cd backend && python test_walk.py 31.861 117.285 30 --mode walk+bus
cd backend && python test_walk.py 31.861 117.285 30 --mode walk+metro+bus

# 公交数据全流程（免费方案，一次性）
cd scripts/utils && python download_hfbus_official.py   # 官方站点/线路
cd scripts/crawlers && python crawl_8684.py             # 8684 有序站序
cd scripts/utils && python load_8684_bus.py             # 匹配坐标 + 落库
cd scripts/utils && python snap_bus_stops.py            # 公交站吸附

# 路网分类重算（如需调整规则后重跑，读缓存，不重新抓取）
python scripts/utils/enrich_roads_tags.py --classify-only
```

---

*本文档为进度快照，后续开发后请更新。*
